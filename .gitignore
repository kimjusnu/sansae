# ── 2026-08-05: 사용자 결정으로 CLAUDE.md·source·job-change·cover-letters 공개 전환 ──
# (이전에는 개인정보 보호를 위해 제외했음 — 복원하려면 git 히스토리 참고)

# 제3자 저작물 (레퍼런스 이미지 — 저작권상 공개 게시 불가)
image*.png

# Claude 메모리 · 임시 파일
/memory/
MEMORY.md
*.tmp
.DS_Store
Thumbs.db

# 의존성/빌드
node_modules/
dist/
.cache/

# wrangler 로컬 상태·빌드 산출물
.wrangler/
.dev.vars

# 증명사진 원본 (가공본만 portfolio/resume/photo.webp로 배포)
/design/photo-source/

# 개인 이력서 원본 PDF (연락처 포함 — 공개 레포 커밋 금지)
/main.pdf

# 이력서 PDF 생성용 임시 Chrome 프로필
/design/.chrome-pdf/
